### <version> — <ISO-date> (<status>)
- <change summary line — distilled from this ship run's ledger rows + git diff>
- <change summary line>

<!--
Insert this block at the TOP of HISTORY.md's "## Changelog" section (newest first).
Sourcing per references/changelog-format.md:
  1. This ship run's ledger rows (POLISH/AUDIT/TIER clusters addressed)
  2. git log since last ship (if the skill is in a repo)
  3. user-described changes
One line per meaningful change. Summarize prose tweaks as "polish pass".
Bump type: patch (fixes) / minor (new capability) / major (breaking).
Status suffix <status>: "shipped" for a normal ship; "initial publish" for a degraded-mode first publish (per changelog-format.md).
-->
